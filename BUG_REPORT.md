# バグ調査報告書

調査日: 2026-09-13

この報告書は、現行コードの未解決9件と、Git履歴で修正を確認できる解決済み3件を記録したものです。合計は12件です。

## 未解決のバグ

| No. | 重要度 | 該当箇所                                                                                         | なぜ悪いか                                                                                                                                                   | 影響・再現条件                                                                                                                      |
| --- | ------ | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 高     | `frontend/src/views/TodosView.vue:374`                                                           | 削除後のローカル一覧更新で、削除対象の`todo.id`ではなく`todo.title`と各要素の`item.id`を比較している。IDとタイトルは異なるため、削除した要素を除外できない。 | タスクを削除すると、API上では削除済みでも画面の一覧には残り続ける。再読み込み時に初めて消える。                                     |
| 2   | 高     | `frontend/src/views/TodosView.vue:552`、`frontend/src/router.ts:21`                              | リンク先は`/task/:id`だが、ルーターが受け付ける詳細画面のパスは`/tasks/:id`である。パスが一致しない。                                                        | 一覧のタスク名を選択すると詳細ではなく404画面になる。                                                                               |
| 3   | 高     | `frontend/src/api/todos.ts:51-53`、`frontend/src/api/todos.ts:79-81`、`backend/src/index.ts:152` | 詳細取得のクライアント型と参照先が`{ todo }`になっている一方、APIは`{ data: { todo } }`を返す。                                                              | 正しいURLを直接開いても、取得後の`todo`が`undefined`になり、詳細画面を表示できない。                                                |
| 4   | 高     | `frontend/src/views/TodosView.vue:111-119`                                                       | 完了件数の算出がモデルに存在しない`completed`プロパティを参照している。正しいプロパティは`isCompleted`である。                                               | 完了タスクがあっても完了件数は常に0件となり、進捗は0%、残タスクは全件数として表示される。                                           |
| 5   | 中     | `frontend/src/views/TodosView.vue:121-125`                                                       | 次の期限を求める並び替えが期限の降順であり、先頭には最も遠い期限が来る。仕様は昇順の先頭である。                                                             | 未完了タスクが複数あると、「次の期限」に最も遅い期限が表示される。                                                                  |
| 6   | 中     | `frontend/src/views/TodosView.vue:213-215`                                                       | 対象日までに完了した件数は`completedAt <= date`で数えるべきだが、実装は`completedAt >= date`である。                                                         | バーンダウン実績が日付に対して逆方向に集計され、過去日・当日の残タスク数が誤る。                                                    |
| 7   | 高     | `backend/src/index.ts:84-90`                                                                     | 登録時の不変条件が`dueDate <= startDate`になっている。要件の「開始日が期限以前」と逆である。                                                                 | 例として開始日`2026-09-10`、期限`2026-09-11`という正常な入力が422で拒否される。一方、期限が開始日より前の不正な入力を通してしまう。 |
| 8   | 高     | `backend/src/index.ts:92-97`、`backend/src/index.ts:210-223`                                     | 更新用スキーマは各日付の形式だけを検証し、既存値と更新値を合わせた後の`startDate <= dueDate`を検証していない。                                               | 片方の日付だけを更新して、開始日が期限より後のタスクを保存できる。ドメイン不変条件が壊れ、ガント表示や集計の前提も崩れる。          |
| 9   | 中     | `backend/src/index.ts:128-143`                                                                   | 公開する一覧応答に、`internal`として保存方式、次の連番、担当者のメールアドレスと部署を含めている。API仕様の一覧応答は`data.todos`のみである。                | 一覧APIを呼べる利用者全員に不要な内部情報・個人に結び付く連絡先情報が露出する。                                                     |

## 解決済みのバグ

解決済み項目の行番号は、修正前コミットにおける行番号です。現在のコードでは修正済みのため、同じ不正な行は存在しません。

| No. | 修正前の該当箇所                                                            | 修正内容                                            | なぜ悪かったか                                                                                                          |
| --- | --------------------------------------------------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 10  | `frontend/src/api/todos.ts:95`（修正前コミット `a7d144c` の親）             | `PUT`を`PATCH`へ変更。                              | バックエンドは`PATCH /api/todos/:id`だけを定義しているため、更新要求が404となり、完了状態・タイトルを変更できなかった。 |
| 11  | `frontend/src/views/TodosView.vue:141-142`（修正前コミット `faeefe3` の親） | 完了・未完了の条件を正しい`isCompleted`判定へ変更。 | 「完了」が未完了タスクを、「未完了」が完了タスクを表示していた。                                                        |
| 12  | `frontend/src/views/TodosView.vue:471`（修正前コミット `cea0590` の親）     | 担当者欄の`type="number"`を`type="text"`へ変更。    | 担当者名は文字列というドメイン仕様に反し、日本語の氏名などを入力できなかった。                                          |

## 確認方法

- APIの既存テストは `vp test` で4件すべて成功した。
- `vp check` はリポジトリ全体に既存の整形不一致があるため失敗した。この調査ではソースコードを変更していない。
- 未解決項目は、画面・API・ドメイン仕様書の実装と契約を突き合わせて確認した。
